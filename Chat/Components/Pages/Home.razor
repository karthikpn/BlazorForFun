@page "/"
@using Microsoft.AspNetCore.Components.Authorization

<PageTitle>Home - Chat App</PageTitle>

<div class="home-container">
    <div class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">
                <span class="chat-icon">💬</span>
                Welcome to Chat App
            </h1>
            <p class="hero-subtitle">
                Connect, communicate, and collaborate in real-time
            </p>
        </div>
    </div>

    <div class="features-section">
        <h2>Why Choose Our Chat App?</h2>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">⚡</div>
                <h3>Real-Time Messaging</h3>
                <p>Instant message delivery powered by SignalR technology</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🔒</div>
                <h3>Secure Authentication</h3>
                <p>Protected by Auth0 enterprise-grade security</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🎨</div>
                <h3>Modern Interface</h3>
                <p>Clean and intuitive design built with Blazor</p>
            </div>
        </div>
    </div>

    <div class="info-section">
        <div class="info-box">
            <h3>About This App</h3>
            <p>
                This is a real-time chat application built with <strong>Blazor Server</strong> and <strong>SignalR</strong>.
                It demonstrates modern web technologies including interactive components, real-time communication,
                and secure authentication.
            </p>
                </div>
            </div>
</div>

<style>
    .home-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem 1rem;
    }

    .hero-section {
        text-align: center;
        padding: 3rem 0;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 1rem;
        margin-bottom: 3rem;
        color: white;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    .hero-content {
        padding: 2rem;
    }

    .hero-title {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 1rem;
        animation: fadeInUp 0.8s ease;
    }

    .chat-icon {
        display: inline-block;
        font-size: 3.5rem;
        margin-right: 1rem;
        animation: bounce 2s infinite;
    }

    .hero-subtitle {
        font-size: 1.5rem;
        opacity: 0.95;
        animation: fadeInUp 1s ease;
    }

    .features-section {
        margin-bottom: 3rem;
    }

    .features-section h2 {
        text-align: center;
        font-size: 2rem;
        margin-bottom: 2rem;
        color: #333;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-bottom: 2rem;
    }

    .feature-card {
        background: white;
        padding: 2rem;
        border-radius: 0.75rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
    }

    .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
    }

    .feature-card h3 {
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
        color: #667eea;
    }

    .feature-card p {
        color: #666;
        line-height: 1.6;
    }

    .action-section {
        text-align: center;
        padding: 3rem 2rem;
        background: linear-gradient(to right, #f8f9fa, #e9ecef);
        border-radius: 1rem;
        margin-bottom: 2rem;
    }

    .welcome-message {
        font-size: 1.5rem;
        margin-bottom: 1.5rem;
        color: #333;
    }

    .login-prompt {
        font-size: 1.5rem;
        margin-bottom: 1.5rem;
        color: #333;
        font-weight: 600;
    }

    .btn-lg {
        padding: 1rem 3rem;
        font-size: 1.25rem;
        border-radius: 0.5rem;
        text-decoration: none;
        display: inline-block;
        transition: all 0.3s ease;
        font-weight: 600;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        color: white;
    }

    .btn-primary:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
    }

    .login-icon {
        margin-right: 0.5rem;
    }

    .arrow {
        margin-left: 0.5rem;
        display: inline-block;
        transition: transform 0.3s ease;
    }

    .btn-primary:hover .arrow {
        transform: translateX(5px);
    }

    .info-text {
        margin-top: 1rem;
        color: #666;
        font-size: 0.9rem;
    }

    .info-section {
        margin-top: 2rem;
    }

    .info-box {
        background: white;
        padding: 2rem;
        border-radius: 0.75rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        border-left: 4px solid #667eea;
    }

    .info-box h3 {
        color: #667eea;
        margin-bottom: 1rem;
    }

    .info-box p {
        line-height: 1.8;
        color: #555;
    }

    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@keyframes bounce {
        0%, 100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-10px);
        }
    }

    @@media (max-width: 768px) {
        .hero-title {
            font-size: 2rem;
        }

        .chat-icon {
            font-size: 2.5rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
        }

        .features-grid {
            grid-template-columns: 1fr;
        }
    }
</style>